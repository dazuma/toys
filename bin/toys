#!/usr/bin/env ruby

begin
  require "toys"
rescue LoadError
  libdir = File.absolute_path(File.join(File.dirname(__dir__), "lib"))
  $:.unshift(libdir)
  require "toys"
end

Toys::Cli.new(include_builtin: true, include_current_config: true).run(ARGV)

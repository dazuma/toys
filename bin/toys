#!/usr/bin/env ruby

begin
  require "toys"
rescue LoadError
  libdir = File.absolute_path(File.join(File.dirname(__dir__), "lib"))
  $:.unshift(libdir)
  require "toys"
end

Toys::Cli.create_standard.run(ARGV)
